class Score {
  constructor() {
    this.hits = 0;
    this.misses = 0;
    this.turn = true;
  }

  // method to hit oponents board at specified coordinates

  // method to add/remove to score

  // method to tally hits and misses

  // method to see who's turn it is - one player is true, other one is false (like heads and tails)

  // method to check if game is over
}
